1. What statement best describes properties in Neo4j ?
-Properties are the key-value pairs used for nodes and relationships.

2. Which of the following is a benefit of using a graph database over a relational db ?
- to perform fast and complex traversals of highly interconnected data

3. Which of the following elements make up a property graph ?
- Nodes
- Labels
- Relationships
- Properties

4. True or false, a relationship can start and end at the same node.
-true

5. What is Ne04j ?
-Graph db

6. What statement best describes Cypher ?
- it is a declarative query language designed for graph pattern matching and traversals.

7. Supports ACID
-true

8. Uses indexes to store relationships between nodes
-false

9. What protocol do the official Ne04j drivers use to access the Neo4j database ?
-Bolt

10. Index free adjacency for a neo4j db ? 
-Nodes and its relations are stored as pointers 
so that access is very fast without the need for an index to travers nodes.

11. To take advantage of the Query cache, what should you do ?
- use params in your query
- ensure the casing of keywords and parameters is followed identically in query  

12. How can you delete a specific node in Neo4j using Cypher ?
Use Match clause to find the node and then DELETE it

13. Which of the following Cypher statements will find people who acted in the movie Toy Story ?
- MATCH (p:Person)-[:ACTED_IN]->(m:Movie) WHERE m.title='Toy Story' return p;
- MATCH (p:Person)-[:ACTED_IN]->(m:Movie {title: 'Toy Story'}) return p;

14. How can you ensure property exists in Cypher query ?
-use the 'IS NOT NULL' operator

15. Which of the following statements best describes the pattern i this MATCH ?
MATCH (p:Person)-[r]->(m:Movie)
-it finds nodes with the label Person with an outgoing relation to a Movie

16. Update the following Cypher statement to find or create a node in the database.
(m:Movie {title: 'Toy Story'})
return m
-MERGE

17. How can you eliminate duplicated rows in the results of a Cypher query ?
-Using the DISTINCT keyword

18. How can optional matches be useful in neo4j ?
-Optional matches allow you to specify patterns that may or may not exist in the graph

19. In the following Cypher statement, what do the parentheses represent ?
MATCH p=()-->() RETURN pairs
- Nodes

20. In the following MATCH clause, which of the elements represents the relationship between 2 nodes ?
MATCH (a)-[b]->(c):
- -[b]->


21. Which of the following best describes the behaviour of MERGE in Neo4j ?
-it matches exisitng patterns or creates new ones if they do not exist.

22. How do you ensure that the same relationship is not created twice between 2 nodes ?
-use Merge KEYWORD when creating relation

23. Say you open up a new db and run the following Cypher command to create a new Movie node
CREATE (:Movie{title: 'Barbie'})
Then, you run the the following MERGE Cypher statement.
MERGE (p:Person {name: "Margot Robbie"})-[:ACTED_IN]-(m:Movie{title: "Barbie"})
How many nodes will exists in your db ?
- 3 Nodes; 1- Person, 2 - Movies

24. Complete the statement:
MATCH (p:Person)-[:Acted_in]->()-[:IN_GENRE]->(:Genre {name: 'Comedy'})
CALL {
 //
	SET p:FunnyActor
} in TRANSACTIONs of 1000 rows
- WITH p


25. How can  you escape special chars such as spaces in db, user and role names ?
-with backticks (')

26. Which of the following Cypher Statements would you use to count the number of nodes with label Person?
-MATCH (n:Person) return count(n)
-RETURN COUNT{(:Person)}

27. Complete the follwoing statement: 
MATCH (:Movie{title: 'The Matrix'})<-[:ACTED_IN]-(p:Person)
- p.name

28. Which Cypher command can be used to list the indexes in a Ne04j db ?
-SHOW indexes

29. Say you have a db with unique constraint on the .name property for the (:Person) label
You run this query twice
MERGE (p:Person {name: "Michelle Yeoh", createdAt: datetime()})
what will happened:
- A Neo.ClientError.Schema.ConstraintViolation error will be thrown

30. Which of the following commands would you use to drop a constraint ?
-DROP CONSTRAINT [constraint_name]

31. Which Cypher query would use a subquery ?
- CALL

32. What is difference between UNION and UNION ALL ?
- UNION returns distinct, UNION ALL - inculde duplicates

33. If you run the query on empty db, which property will exist in node ?
MERGE (m:Movie {title: "Toy Story"})
ON CREATE SET m.releases = 1995
ON MATCH SET m.tagline = "The ..."
SET m.imdbRating = 8.3
- title
- released
- imdbRating

34. What is primary purpose of the WITH ?
- to divide the query into multiple parts, 
allowing intermediate results to be passed to the next part of the query

35.  In a query requiring pagination, why is it essential to use an ORDER BY before skip, limit?
-it ensures consistent and predictable results across different pages

36. Which of the following statements best describes the difference between Cypher and GraphQL:
-Cypher is declarative graph query

37. Why would Tom Hanks appear in this Cypher statement 
MATCH (p:Person)-[:ACTED_IN]->(m)
MATCH (m)<-[:ACTED_IN]-(p2)
RETURN p2.name AS coactor

but not in this one ?
MATCH (p:Person)-[:ACTED_IN]->(m),
(m)<-[:ACTED_IN]-(p2)
RETURN p2.name AS coactor

- relations are unique within the scope of a single Match

38. What si differene between EXPLAIN and PROFILE ?
-EXPLAIN provides estimates, while PROFILE provides exact steps and number of rows retrieved


39. How to improove ? 
MATCH (n)--(m)
return *;

- define a label for one of the nodes
- define a realtionship type
- define a direction for the relationship


40. Suppose we want to return a list of all movies for each actor whose name contains "Tom".
What is wrong here ?
 MATCH (a:Person)
 WHERE a.name CONTAINS "TOM"
 WITH a, a.name AS actorName
 CALL 
 {
	MATCH (a)-[:ACTED_IN]->(m:Movie)
	RETURN collect(m.title) as movies
 }
 return actorName, movies

- you must specify WITH in the subquery for any variables you are passing to the subquery

41. How many records will the following Cypher statement return ?
UNWIND range(1, 3) AS outer
CALL {
	with outer
	unwind range(1,3) as inner
	return inner
}
return outer, inner
- 9

42. Why is it recommended to avoid unnecessary labels in query patterns for better performance ?
- Labels on non-anchor nodes force a label check, which may not be necessary.

43. Traversal:
- depth-first

44. Given the following domain question.
Which customers have purchased Product X ?
Which of the following could you represent  as relationships in your data model ?
- Purchased

45. What are some things to look for as red flags to investigate in a Cypher query plan ?
- AllNodes Scan
- NodeByLabelScan with a lot of DB hits
- Placement of eager operators
- Reading property values early in the query plan


46. Given the following domain question:
What parts of type X are required to make product Y ?
Which of the following elements could you represent as nodes in your data model ?
- Type
- Part
- Product

47. Intermediate nodes:
You have a graph consisting of (:Customer) nodes with :ORDERED relationships to (:Products)
Instead of using a direct relationship between Customer and Product to represent an order,
what is the benefit of extracting this relationship into an intermediate node Order ?
Which of the following optimizations involves enhancing the data model by extracting the 'ordered'
relationship into a separate node ?
- it enables linking the order to multiple entities, such as products and shipping companies.

48. You have a database consisting of (:Person) nodes connected to (:Movie) nodes through 
:ACTED_IN and :DIRECTED relationships ?
What is the benefit of adding additional (:Actor) labels for any (:Person) node with an :ACTED_IN relation ?
-it provides a faster way to find a subset of (:Person) nodes

49. In modeling, the use of bi-directional relationships is a good practice when:
- The semantics of the relationship in 1 direction is different from the other direction

50. What is the recommended limit for lables in node ?
- 4 labels

51. Before you start a query tuning exercise, you must make sure that
 set of queries you will be measuring represents the correct syntax.
 You can use EXPLAIN to ensure the that unnecessary NodeByLabelScan steps occur.
 For each query, you should confirm that:
 -An index will be used as expected
 -Use correct names for labels, property keys, and property values
 -Use correct relationship types
 
52. You are building a recommendation engine for an online store.
The store organizes products into departaments, each containing sub-categories.
An excerpt of their category list is as follows:
Which of the following actions should you take when modeling the data ?
- Create a hierarchy of Category nodes


53. You are building a recommendation engine for onlike store.
Some of their products are perishable and need to be sold within a certain timeframe.
Managers would like to find a list of perishable products for stock control purposes.
Which of the following actions should you take when modeling the data ?
-Add :Perishable label to perishable products  

54. You receive reprts from users that your website is slow.
You narrow down the problem to a single Cypher statement used on the home page.
	MATCH (c:Customer {id: $customerId})-[:PLACED|ORDERED*2]->(p:Product)
	return p.name
	order by p.name ASC
After running the EXPLAIN command, you spot a NodeByLabelScan as the 1st step in the query plan.
Which of the following actions should you take to improve the performance of the query ?

- Add an index to the :Customer.id property

55. How do you create a unique constraint on the email property for any node with the label User ?
- CREATE CONSTRAINT FOR (u:User) REQUIRE (u.email) IS UNIQUE


56. Which of the following statements is correct ?
 - Neo4j allows for an optional schema
 
57. True or False - You can create index on nodes or relationships
- True

58. Complete the Cypher statement below to create an existence constraint on the 
rated property on the ()-[:RATED]->() relation

CREATE CONSTRIANT rated_rating
FOR ()-[r:RATED]-() REQUIRE r.rating ////

- IS NOT NULL

59. Which of the following indexes are available in Neo4j ?
-TEXT
-RANGE
-VECTOR

60. wHICH OF THE FOLLOWING cYPHER Statements will visualize your db schema in Workspace or Neo4j Browser ?
- CALL db.schema.visualization()


61. What typ eof constriant can you use to ensure that 2 or more properties are unique
and exist for any given label ?
- Node Key Constraint

62. How do you create a RANGE index on a relationship property ?
- CREATE RANGE INDEX

63. What makes fulltext and vector indexes different from other indexes in neo4j ?
They must be queried by a calling a procedure.

64. Complete the Cypher statement below to query a fulltext index 
called movieInfo with the value in the parameter $query.

- CALL db.index.fulltext.queryNodes

65. Complete the Cypher statement below to create a Vector index on the :Movie.embedding property
- CREATE VECTOR INDEX

66. Which Cypher command woul you use to load data from a CSV file ?
-LOAD CSV

67. In a complex data import scenario involving both nodes and relationships, 
which of the following sequence of steps is recommended ?
- Import nodes first, then relationships

68. Which of the following statements applies when using the LOAD CSV statement ?
-All values are cast as a string
-You must cast any values other than a string using the appropriate Cypher function

69. Complete the following Cypher statement to instruct Neo4j that the 1st row in the CSV file contains headers
- WITH HEADERS

70. If you omit WITH HEADERS from a LOAD CSV statement, which of the following will happen ?
- each row will be treated as a list


71. Complete the Cypher statement below to execute the LOAD CSV command in batches of 1000 rows
- IN TRANSCATIONS OF 1000 ROWS

72. What is the role of a driver in NEO4J ?
- To establish a connection with the neo4j db

73. Which of the following protocols are valid ?
- bolt+s
- ne04j+s
- neo4j+ssc

74. What is the default port for driver connection via BOLT ?
- 7687

75. When using a driver, a statement consists of:
- records

76. Complete the following Python code. How to check connection ?
- verify_authentication()

77. When Driver via ne04j+ssc:
- The driver will establish an encrypted connection but it will not verify SSL certificate

78. When using a driver, which forms of transaction functions will be automatically retired on failure ?
- Transaction functions

79. To make read query with Python Driver:
- execute_query



